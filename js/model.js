class Model {
    constructor(){
        this.view = null;
        this.todos = [];
    }

    setView(view) {
        this.view = view;
    }

    setTodos(){
        this.todos;
    }

    addTodo(title, description){
        console.log(todo);
    }
}